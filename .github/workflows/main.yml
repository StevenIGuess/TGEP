name: Build and Run TGEP/Sandbox
on: [push]
    
jobs:
  build_and_test:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v1
      - name: BuildAll
        shell: pwsh
        run: .\BuildAll.bat
